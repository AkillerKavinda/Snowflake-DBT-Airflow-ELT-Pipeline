version: 2

models:
  - name: dim_customers
    description: Customer dimension table with customer profile and segment information.
    columns:
      - name: customer_id
        description: Unique identifier for each customer.
        tests:
          - unique
          - not_null

      - name: market_segment
        description: Customer market segment.
        tests:
          - not_null